Script -> Text File -> Series of commands that Linux Shell (Bash) will execute 

The Shell scripts it actually follow a method of writing commands , 
that is simply called as shebang.

----------------------------------------------------------------------------------------

when we create any file with nano , then by default is has no executable permission
we need to give persions 

need to add permission for execute 
chmod +x file_name.sh